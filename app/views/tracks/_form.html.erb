<%= form_for(@tag ? [@tag, @track] : @track, html: {id: 'track_form'}) do |f| %>
  <% if @track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@track.errors.count, "error") %> prohibited this track from being saved:</h2>

      <ul>
      <% @track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, placeholder: t('views.tracks.form.name') %>
    <%= f.text_field :tag_name, placeholder: t('views.tracks.form.group') %>
    <%= f.file_field :attachment %>
    <%= f.submit t('views.tracks.form.save') %>
  </div>
<% end %>
